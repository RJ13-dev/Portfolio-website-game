# Core
# 5.0.6 shipped a potential SQLi (CVE-2024-42005) + several ReDoS issues;
# stay on the latest patched 5.0.x. Run `pip-audit` to confirm the newest.
Django>=5.0.11,<5.1
djangorestframework==3.15.2

# Database
psycopg2-binary==2.9.9
dj-database-url==2.1.0

# Auth (JWT for the game API)
djangorestframework-simplejwt==5.3.1

# (anthropic removed — the chatbot uses Google Gemini via a plain HTTPS call,
#  so the unused SDK was dropped to shrink the attack surface.)

# Production server + static files
# gunicorn 22.0.0 had HTTP request smuggling (CVE-2024-6827); 23+ fixes it.
gunicorn>=23.0.0
whitenoise==6.7.0

# AWS S3 static/media storage
django-storages==1.14.3
boto3==1.34.122

# Redis cache (leaderboard + future Channels)
django-redis==5.4.0
redis==5.0.4

# Config
python-dotenv==1.0.1

# CORS (so the game frontend can call the API)
django-cors-headers==4.3.1

# API docs (OpenAPI/Swagger)
drf-spectacular==0.27.2

# Testing
pytest==8.2.1
pytest-django==4.8.0
pytest-cov==5.0.0
factory-boy==3.3.0

# Linting
flake8==7.0.0
